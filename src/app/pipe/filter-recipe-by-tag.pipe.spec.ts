import { FilterRecipeByTagPipe } from './filter-recipe-by-tag.pipe';

describe('FilterRecipeByTagPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRecipeByTagPipe();
    expect(pipe).toBeTruthy();
  });
});
