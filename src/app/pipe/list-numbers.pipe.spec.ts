import { ListNumbersPipe } from './list-numbers.pipe';

describe('ListNumbersPipe', () => {
  it('create an instance', () => {
    const pipe = new ListNumbersPipe();
    expect(pipe).toBeTruthy();
  });
});
